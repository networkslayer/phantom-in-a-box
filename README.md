# phantom-in-a-box

In order to execute phantom-in-a-box you need to execute in a virtual environment. From the bash prompt execute:

pip install virtualenv && virtualenv -p python3 venv && source venv/bin/activate && pip install -r requirements.txt

This command ensures virtualenv is installed, along with the reuirements for Phantom-in-a-box
