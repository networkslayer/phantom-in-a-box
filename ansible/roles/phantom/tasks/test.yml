- hosts: all
  gather_facts: no
  become: yes
  tasks:
        # install a list of apps using RPMs. URL's can be found at https://repo.phantom.us/phantom/4.8/apps/x86_64/

  - name: Testing workflow
    uri:
      user: admin
      url: https://127.0.0.1/rest/workflow_template
      password: "Phantom-!n-a-b0x!"
      validate_certs: no
      force_basic_auth: yes
      method: POST
      body: {"name":"test1","description":"","phases":[{"id":"new-phase4","name":"phase1","tasks":[{"id":"new-task5","name":"task1","description":"tsak1 desc","owner":null,"is_note_required":true}]}],"is_default":false,"is_note_required":true}
      body_format: json